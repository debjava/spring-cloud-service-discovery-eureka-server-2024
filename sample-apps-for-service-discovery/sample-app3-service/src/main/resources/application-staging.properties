# you can also mention the port and context path in the config properties file for cloud config
server.port=8080
server.servlet.contextPath=/sample-app2-service

# Mention the name of the microservice below
spring.application.name=sample-app2-service
spring.config.import=optional:configserver:http://localhost:8888
